# Requirements for SEIR-PINN Grid Search
# Install with: pip install -r requirements_grid_search.txt

# Core dependencies (should already be installed)
torch>=1.10.0
numpy>=1.20.0
scikit-learn>=0.24.0
matplotlib>=3.3.0

# Grid search specific
tqdm>=4.60.0
pandas>=1.3.0
tabulate>=0.8.9
